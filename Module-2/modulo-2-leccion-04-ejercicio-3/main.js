'use strict';


function price(a) {
  return (a) ;
}
const resultPrice = (20);
const priceIva = (20 * (21 / 100) );
const priceTotal = resultPrice + priceIva;
console.log(`El precio sin IVA ${resultPrice} , el IVA (21%) ${priceIva} y el total ${priceTotal}`);