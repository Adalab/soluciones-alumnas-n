'use strict';

const theName = 'lucia';

const nameToCompare = 'ANDREA';

if (theName.toLowerCase()===nameToCompare.toLowerCase()) {
    console.log('Bienvenida! :D');
}
else {
    console.log('Lo siento pero el usuario que has introducido no está registrado');
}